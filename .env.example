# Application settings
NEXT_PUBLIC_APP_NAME=FreeAPI Directory
NEXT_PUBLIC_APP_URL=http://localhost:5000

# Server settings
PORT=5000
HOST=0.0.0.0

# Optional MongoDB connection string (when migrating from in-memory storage)
# MONGODB_URI=mongodb://username:password@localhost:27017/freeapi-directory

# Admin credentials
# These are used as fallback when no admin user exists in the database
# ADMIN_USERNAME=admin
# ADMIN_PASSWORD=admin123

# Set to 'true' when generating a static export for shared hosting
# EXPORT=false
